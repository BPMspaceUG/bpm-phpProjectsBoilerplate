services:
  ${PROJECT_NAME}_test:
    build:
      context: .
      dockerfile: Dockerfile.Apache.TEST.PROD
    container_name: ${PROJECT_NAME}_test
    hostname: ${PROJECT_NAME}_test
    restart: always
    environment:
      - IMAGE_DATE=${IMAGE_DATE}
      - IMAGE_VERSION=${IMAGE_VERSION}
      - PUSH_SHA=${PUSH_SHA}
      - APP_ENV=TEST
      - APP_NAME=${APP_NAME}
      - LOCALLOG=true
      - REDIS_SERVER=${PROJECT_NAME}-redisserver_test
      - REDIS_PORT=6379
      - REDIS_CACHETIME=3600
      - DB_HOST=${PROJECT_NAME}-mariadb_test
      - DB_NAME=${MYSQL_DATABASE}
      - DB_USER=${MYSQL_USER}
      - DB_PASSWORD=${MYSQL_PASSWORD}
    working_dir: /var/www/html
    depends_on:
      - ${PROJECT_NAME}-mariadb_test
      - ${PROJECT_NAME}-redisserver_test
    networks:
      - ${PROJECT_NAME}_test
      - caddy
    labels:
      caddy: ${PROJECT_NAME}.test.bpmspace.net
      caddy.reverse_proxy: "{{upstreams http}}"

  ${PROJECT_NAME}-mariadb_test:
    image: mariadb:latest
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci --sql_mode=""
    container_name: ${PROJECT_NAME}-mariadb_test
    hostname: ${PROJECT_NAME}-mariadb_test
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - MYSQL_DATABASE=${MYSQL_DATABASE}
      - MYSQL_USER=${MYSQL_USER}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD}
    volumes:
      - ${PROJECT_NAME}-mariadb-data_test:/var/lib/mysql
    networks:
      - ${PROJECT_NAME}_test

  ${PROJECT_NAME}-phpmyadmin_test:
    image: phpmyadmin:latest
    container_name: ${PROJECT_NAME}-phpmyadmin_test
    hostname: ${PROJECT_NAME}-phpmyadmin_test
    restart: always
    environment:
      - PMA_HOST=${PROJECT_NAME}-mariadb_test
      - MEMORY_LIMIT=1516M
      - UPLOAD_LIMIT=1024M
    depends_on:
      - ${PROJECT_NAME}-mariadb_test
    networks:
      - ${PROJECT_NAME}_test
      - caddy
    labels:
      caddy: pma-${PROJECT_NAME}.test.bpmspace.net
      caddy.reverse_proxy: "{{upstreams 80}}"

  ${PROJECT_NAME}-redisserver_test:
    image: redis/redis-stack:latest
    container_name: ${PROJECT_NAME}-redisserver_test
    hostname: ${PROJECT_NAME}-redisserver_test
    restart: always
    volumes:
      - ${PROJECT_NAME}-redis-data_test:/data
    networks:
      - ${PROJECT_NAME}_test

  ${PROJECT_NAME}-phpredisadmin_test:
    image: erikdubbelboer/phpredisadmin
    container_name: ${PROJECT_NAME}-phpredisadmin_test
    hostname: ${PROJECT_NAME}-phpredisadmin_test
    restart: always
    environment:
      - REDIS_1_HOST=${PROJECT_NAME}-redisserver_test
      - REDIS_1_NAME=${APP_NAME}
      - ADMIN_USER=${REDIS_ADMIN_USER}
      - ADMIN_PASS=${REDIS_ADMIN_PASS}
    networks:
      - ${PROJECT_NAME}_test
      - caddy
    labels:
      caddy: pmr-${PROJECT_NAME}.test.bpmspace.net
      caddy.reverse_proxy: "{{upstreams 80}}"

volumes:
  ${PROJECT_NAME}-mariadb-data_test:
    driver: local
  ${PROJECT_NAME}-redis-data_test:
    driver: local

networks:
  ${PROJECT_NAME}_test:
    driver: bridge
  caddy:
    external: true
